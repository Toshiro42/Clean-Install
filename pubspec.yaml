name: clean_install
description: Clean Install - Security utility

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.0
  flutter_dotenv: ^5.1.0
  url_launcher: ^6.2.5

flutter:
  uses-material-design: true
  assets:
    - .env
